<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.oneapp.eddy.myapp.com.oneapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_myicon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:roundIcon="@mipmap/ic_myiconround"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityInicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivitySecond" />
        <activity android:name=".ActivityEdit" />
        <activity android:name=".ActivityListBusiness" />
    </application>

</manifest>